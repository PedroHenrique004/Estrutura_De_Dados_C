#include <stdio.h>


int main (){

    int num, quad, qnt;
    quad = 0;

    printf("Entre com um numero: ");
    scanf("%d", &num);
    qnt = num;

    // 3 + 3 + 3 = 9

        for (int i = 0; i = qnt; i++)
        {
            num += num;
        }

        quad = num;

    printf("O quadrado do número %d é %d", num, quad);
    
    

    return 0;
 }